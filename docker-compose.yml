version: "3.9"
services:
  server:
    build: .
    image: "servermanager:server"
    command: ["java", "-jar", "server.jar"]
    container_name: "server"
    networks:
      default:
        ipv4_address: "192.168.76.2"
    ports:
      - "5976:5976/tcp"
    volumes:
      - "./run/server/client.json:/app/client.json"
      - "./run/server/server.json:/app/server.json"
networks:
  default:
    name: "servermanager"
    ipam:
      driver: "default"
      config:
        - subnet: "192.168.76.0/24"